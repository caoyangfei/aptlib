
项目说明：
   总共五个library
   1  annotation（注解 java）
   2  api（编译代码所依赖接口 library）
   3  complier （编译生成文件 java）
   4  aop切片包（基础切片功能）
   5  插件
配置说明：
   因为annotation，api，complier使用时是联合使用所以插件中配置了三个的依赖。

配置路由时：
   在根目录gradle中配置如下：
   1.    buildscript {
            repositories {
                maven {
                      url 'https://jitpack.io'
                }
                google()
                jcenter()
            }
            dependencies {
                  classpath 'com.android.tools.build:gradle:3.3.1'
                 //插件
                 com.github.caoyangfei.aptlib:plugin:V1.1.1.2019_release_01
              }
        }
   2.   配置版本，有可能插件中版本未更新，最好配置版本，同在根目录gradle
        ext {
            apiVersion = "V1.1.1.2019_release_02"
            annotationVersion = "V1.1.1.2019_release_02"
            complierVersion = "V1.1.1.2019_release_02"
        }
   3.   使用
   在所在业务moudle gradle直接添加
       import com.flyang.plugin.router.RouterPlugin
       apply plugin: RouterPlugin
   这样annotation，api就能获取下来。

使用aop时配置
   1.  插件配置同路由配置1
   2.  使用
   在所在业务moudle gradle直接添加
      import com.flyang.plugin.aspectj.AspectjPlugin
      apply plugin: AspectjPlugin
   aop包中已经写了几个切片方法，可以仿照写检查登陆状态等。
   使用aop library的方法：
   implementation 'com.github.caoyangfei.aptlib:aop:V1.1.1.2019_release_01'

   注：如果不使用aop切片编程，不要添加此配置，会找不到注入的类


使用说明：
    使用方法见demo